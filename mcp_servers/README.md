# MCP boundaries

- `personal`: local-only private tools;
- `trust_domain`: membership, presence, and audience-scoped claims;
- `mailbox`: protocol envelope relay;
- `audit`: append-only events and read-only traces.

No external identity may route to the personal server.
