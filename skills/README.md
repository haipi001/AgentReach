# Skills

Each skill is a versioned engineering contract. `SKILL.md` specifies triggers, inputs, outputs, dependencies, failure behavior, safety boundaries, and verification. Runtime implementations arrive in subsequent slices without changing these public contracts.
